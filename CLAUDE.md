# Crossfade

Move a playlist from one music service to another. Spotify and YouTube Music, both directions.

Planning stage. Nothing is implemented yet.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `paritosh4495/crossfade`, driven through the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its role name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and one `docs/adr/` at the repo root. See `docs/agents/domain.md`.
